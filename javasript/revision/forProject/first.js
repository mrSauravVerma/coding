var roll_num = 329;
{
    let name = "saurav";
    let erp_id = 80;
    console.log(`my name is ${name}`);
    console.log(`my erp is : ${erp_id}`);
    console.log(`my roll number is : ${roll_num}`)

    let num = 40;
    console.log(`factor of 5 : `)
    for (let i = 1; i <= num; i++) {
        if (num % i == 0) {
            console.log(i);
        }
    }

    console.log(`name[0] : ${name[0]}`);
    console.log(`length of string name : ${name.length}`);
}w