#include<stdio.h>
int main (){
    int size;
    printf("Enter the size of array : ");
    scanf("%d",&size);
    int i,arr[size];
    printf("Enter the element of array : ");
    for( i=0 ; i<size ; i++){
        scanf("%d",&arr[i]);
    }
    // bubble sort
    int j;
    for(i=0 ; i<size ; i++){
        for(j=0 ; j<size ; j++){
            
        }
    }
    return 0;
}